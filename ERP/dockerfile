# # Stage 1: Build the application using Maven
# FROM maven:3.9.11-eclipse-temurin-21 AS build
#
# # Set working directory
# WORKDIR /app
#
# # Copy pom.xml and download dependencies (for caching)
# COPY pom.xml .
# RUN mvn dependency:resolve -B
#
# # Copy source code and build the JAR
# COPY src src
# RUN mvn clean package -DskipTests
#
# # Stage 2: Create a slim runtime image
# FROM eclipse-temurin:21-jre
#
# # Set working directory
# WORKDIR /app
#
# # Install fonts for flying-saucer-pdf/batik to avoid PDF rendering issues
# RUN apt-get update && apt-get install -y libfreetype6 fontconfig && rm -rf /var/lib/apt/lists/*
#
# # Copy the built JAR from the build stage
# COPY --from=build /app/target/*.jar app.jar
#
# # Expose the port (Render will override with $PORT)
# EXPOSE 8081
#
# # Run the JAR file
# CMD ["java", "-jar", "app.jar"]


# Stage 1: Build the application using Maven
FROM maven:3.9.9-eclipse-temurin-21 AS build

# Set working directory
WORKDIR /app

# Copy pom.xml and download dependencies (for caching)
COPY pom.xml .
RUN mvn dependency:resolve -B

# Copy source code and build the JAR
COPY src src
RUN mvn clean package -DskipTests

# Stage 2: Create a slim runtime image
FROM eclipse-temurin:21-jre

# Set working directory
WORKDIR /app

# Install fonts for flying-saucer-pdf/batik
RUN apt-get update && apt-get install -y libfreetype6 fontconfig && rm -rf /var/lib/apt/lists/*

# Copy the built JAR from the build stage
COPY --from=build /app/target/*.jar app.jar

# Expose the port
EXPOSE 8080

# Run the JAR file
CMD ["java", "-jar", "app.jar"]